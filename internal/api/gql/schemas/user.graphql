type User {
    id: UUID!
    email: String!
    isActive: Boolean!
    createdAt: DateTime!
}

input UserInput {
    userID: UUID!
}


extend type Query {
    me: User! @Auth
    user(input: UserInput!): User! @Auth
}
