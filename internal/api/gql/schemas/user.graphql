type User {
    id: UUID!
    email: String!
    isActive: Boolean!
    createdAt: DateTime!
}

input UserInput {
    userID: UUID!
}


extend type Query {
    me: User! @isAuthenticated
    user(input: UserInput!): User! @isAuthenticated
}
